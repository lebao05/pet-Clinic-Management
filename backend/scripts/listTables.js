// This diagnostic script has been removed as part of test cleanup.
// If you need to run database visibility checks again, recreate a small script
// in `backend/scripts` that loads the backend .env and queries `sys.tables`.

// Placeholder so any tooling expecting this path won't fail.
module.exports = {};
